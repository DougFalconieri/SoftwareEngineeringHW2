git init
git add README.rd
git commit -m "Commit 0"
git checkout master
git add README.rd
git commit -m "Commit 1"
git add README.rd
git commit -m "Commit 2"
git checkout master
git add README.rd
git commit -m "Commit 10"
